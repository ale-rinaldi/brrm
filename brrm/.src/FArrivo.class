' Gambas class file


PUBLIC SUB Form_Open()

  Label1.Text = ""
  FArrivo.Center()

END

PUBLIC SUB setOrario(Orario AS String)
  
  Label1.Text = Orario
  
END

PUBLIC SUB Label1_MouseDown()

  

END

PUBLIC SUB Button1_Click()

DIM Numero AS Integer
DIM Confermato AS Boolean
Confermato = FALSE
Numero = 0
TRY Numero = TextBox1.Text
IF Numero > 0 AND Numero < 10000 THEN 
IF FMain.LEquipaggi.Find(Numero) < 0 THEN 
FMain.LEquipaggi.Add(Numero)
ENDIF 
IF FMain.ReturnArrivo(Numero) <> 0 THEN 
IF Message.Question("L'equipaggio " & Numero & " risulta essere già arrivato! Si desidera sostituire il suo orario di arrivo con questo?", "Sì", "No") = 1 THEN
Confermato = TRUE
ENDIF 
ELSE 
Confermato = TRUE
ENDIF 
IF Confermato = TRUE THEN 
FMain.SetArrivo(Numero, Label1.Text)
FArrivo.Close
ENDIF 
ELSE 
Message("Numero non riconosciuto!")
ENDIF 
END

PUBLIC SUB Button2_Click()

  IF Message.Question("Sei sicuro di voler annullare l'inserimento? L'orario di arrivo verrà perso definitivamente.", "Annulla l'inserimento", "Continua a inserire") = 1 THEN
    FArrivo.Close
  ENDIF

END
